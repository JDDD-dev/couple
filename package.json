{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --filter=app --parallel --no-cache",
    "lint": "turbo run lint --parallel",
    "docs": "turbo run dev --filter=web --parallel --no-cache",
    "prismaf": "pnpm --filter=app exec prisma format",
    "format": "pnpm prettier"
  },
  "devDependencies": {
    "eslint": "8.44.0",
    "eslint-config-custom": "workspace:*",
    "prettier": "3.0.0",
    "turbo": "1.10.7"
  }
}