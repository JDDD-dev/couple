{
    "private": true,
    "scripts": {
      "build": "turbo run build",
      "dev": "turbo run dev --parallel --no-cache",
      "lint": "turbo run lint"
    },
    "devDependencies": {
      "eslint-config-custom": "workspace:*",
      "eslint": "8.19.0",
      "prettier": "2.7.1",
      "turbo": "1.3.1"
    }
  }